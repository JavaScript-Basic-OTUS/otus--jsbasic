/**
 * @typedef User
 * @property {string} id
 * @property {string} name
 * @property {number} age
 * @property {boolean} isActive
 */

interface User {
  id: string;
  name: string;
  age: number;
  isActive: boolean;
}
